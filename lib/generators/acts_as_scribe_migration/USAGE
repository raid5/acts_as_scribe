Description:
    The acts_as_scribe_migration generator creates a migration to add the
    specified table to your database.

Example:
    rails generate acts_as_scribe_migration
	
	      Migration:  db/migrate/{TIMESTAMP}_create_scribe_activities.rb